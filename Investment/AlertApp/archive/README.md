# Archived AlertApp scripts (superseded)

These monitors are **not** the supported daily alert path.

**Use instead:** `AlertApp\backgroundAlert1.py` via `start_stock_alert.bat`  
**Alternative (CallMeBot/Twilio):** root `stock_whatsapp_monitor.py` with `config.ini`

| File | Reason archived |
|------|-----------------|
| `backgroundAlert.py` | Older Green API loop; hardcoded credentials removed from source |
| `personalInvestAlert.py` | Browser-based `pywhatkit` approach |
